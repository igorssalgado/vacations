
<%= form_with(model: vacation) do |form| %>
  <% if vacation.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(vacation.errors.count, "error") %> prohibited this vacation from being saved:</h2>

      <ul>
        <% vacation.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
      <tr>
        <td><%= vacation.first_name %></td>
        <td><%= vacation.last_name %></td>
        <td><%= vacation.email %></td>
        <td><%= vacation.vacation_days %></td>
        <td><%= vacation.starting_date %></td>
        <td><%= vacation.ending_date %></td>
        <%
          puts vacation.bonus
          if vacation.bonus == "1"
            vacation.bonus = "Y"
          else
            vacation.bonus = "N"
          end
        %>
        <td><%= vacation.bonus.to_s%></td>
        <td><%= link_to "Edit", edit_vacation_path(vacation), class:"btn btn-secondary btn-sm" %></td>
        <%# <td><%= link_to "X", destroy_vacation_path(vacation)</td>  %>
      </tr>
<% end %>
